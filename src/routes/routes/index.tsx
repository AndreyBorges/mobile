import { NavigationContainer } from '@react-navigation/native'
import { AppRoutes } from '../'

const Routes = () => {
  return (
    <NavigationContainer>
      <AppRoutes />
    </NavigationContainer>
  )
}

export default Routes
